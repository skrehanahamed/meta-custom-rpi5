# Audio stability configuration: enforces 1024 quantum (21.3ms)
# to prevent HDMI sink starvation and buffer underruns.
context.properties = {
    default.clock.rate = 48000
    default.clock.quantum = 1024
    default.clock.min-quantum = 1024
    default.clock.max-quantum = 1024
}
